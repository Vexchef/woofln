app = "woofln"  # Must be globally unique
primary_region = "lax"  # or "iad", "ams", etc.

[build]
  builder = "paketobuildpacks/builder:base"
  
[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = false  # Keeps bot running 24/7
